# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Env/config (if added later)
.env
.env.*

# Node/PHP vendors (if used later)
node_modules/
vendor/

# Build artifacts
dist/
.cache/

# Images cache versions are handled via query strings; keep originals committed
